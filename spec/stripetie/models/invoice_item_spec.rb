require 'spec_helper'

describe Stripetie::Models::InvoiceItem do
  context "scoping" do
    it "can be sorted by company ID" do
    end

    it "can be sorted by customer ID" do

    end

    it "can be sorted by parent invoice ID" do
    end

    it "can be sorted by custom metadata parameter ID" do
    end
  end

  it "can be synced from Stripe" do

  end

  it "calculates the discount amount for a given invoice_item"
end
